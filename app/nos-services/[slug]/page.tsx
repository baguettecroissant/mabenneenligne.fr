import type { Metadata } from "next"; import Link from "next/link"; import { notFound } from "next/navigation"; import { CtaBand, PageHero, SiteShell } from "../../_components/SiteShell"; import { prices, services } from "../../_data/content";
type ServiceKey=keyof typeof services;
export function generateStaticParams(){return Object.keys(services).map(slug=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=services[slug as ServiceKey];if(!s)return{};return{title:s.title,description:`${s.title} livrée sous 24 à 48h. Devis gratuit et prix tout compris partout en France.`}}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services[slug as ServiceKey];if(!s)notFound();return <SiteShell><PageHero eyebrow="Location nationale, service local" title={<>{s.title}. <em>Livraison 24h.</em></>} text={`${s.waste}. Recevez un tarif tout compris et la confirmation de disponibilité dans votre ville.`}><Link className="button" href="/devis">Obtenir mon devis →</Link></PageHero><section className="content-section"><div className="container split-content"><div className="service-page-image" style={{backgroundImage:`url(${s.image})`}}/><div><span className="eyebrow">Déchets acceptés</span><h2>La benne adaptée à votre chantier</h2><p>{s.waste}. Votre conseiller vérifie la compatibilité de chaque matériau avant la réservation afin d’éviter tout refus au centre de traitement.</p><div className="feature-list">{["Livraison et enlèvement inclus","Mise à disposition 7 jours","Traitement en centre agréé","Conseiller dédié du lundi au samedi"].map(x=><div key={x}><span>✓</span><strong>{x}</strong></div>)}</div></div></div></section><section className="content-section alt"><div className="container"><div className="section-heading centered"><span className="eyebrow">Choisissez votre volume</span><h2>Des bennes de 8 à 30 m³</h2><p>Le volume disponible dépend du type de déchets et des contraintes d’accès.</p></div><div className="simple-grid four">{prices.slice(0,4).map(p=><article className="info-card number-card" key={p.volume}><span>{p.volume}</span><h3>À partir de {slug.includes("gravats")?p.gravats:slug.includes("dib")?p.dib:slug.includes("verts")?p.verts:p.bois}</h3><p>Tarif indicatif HT. Votre devis confirme le tonnage inclus et le prix exact pour votre ville.</p></article>)}</div></div></section><CtaBand/></SiteShell>}
