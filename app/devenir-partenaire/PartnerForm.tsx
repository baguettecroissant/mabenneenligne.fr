"use client"; import { FormEvent, useState } from "react";
export function PartnerForm(){const [status,setStatus]=useState("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("sending");const form=new FormData(e.currentTarget);try{const r=await fetch("/api/partners",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});if(!r.ok)throw new Error();setStatus("done")}catch{setStatus("error")}}if(status==="done")return <div className="form-success"><span>✓</span><h2>Candidature envoyée</h2><p>Notre responsable réseau vous contacte rapidement pour étudier votre zone et vos disponibilités.</p></div>;return <form className="partner-form" onSubmit={submit}><h2>Parlons de votre activité</h2><p>Réponse de notre équipe réseau sous 48 heures ouvrées.</p><div className="form-grid"><label>Nom de l’entreprise *<input name="entreprise" required/></label><label>Nom du responsable *<input name="responsable" required/></label><label>Téléphone *<input name="telephone" type="tel" required/></label><label>Email *<input name="email" type="email" required/></label><label className="span-2">Départements couverts *<input name="departements" placeholder="Ex. 59, 62, 80" required/></label><label className="span-2">Types de bennes disponibles<div className="checkbox-grid">{["8 m³","10 m³","15 m³","20 m³","30 m³"].map(x=><label key={x}><input name={`benne_${x}`} type="checkbox"/> {x}</label>)}</div></label><label className="span-2">Déchets acceptés<div className="checkbox-grid">{["DIB","Gravats","Déchets verts","Bois"].map(x=><label key={x}><input name={`dechet_${x}`} type="checkbox"/> {x}</label>)}</div></label><label className="span-2">Message / Grille tarifaire<textarea name="message" rows={4}/></label></div>{status==="error"&&<p className="form-error">L’envoi a échoué. Écrivez à partenaires@mabenneenligne.fr.</p>}<button className="button" style={{width:"100%"}} disabled={status==="sending"}>{status==="sending"?"Envoi…":"Envoyer ma candidature →"}</button></form>}
